node_modules/
.env

# Local VS Code workspace config (tasks, scripts) -- not committed
.vscode/

# Per-operator publish config (contains hub owner token)
scripts/publish.env
scripts/*-build-counter
scripts/channel.txt

# Local cross-compiled build output used by the dev publish script
dist/

# Compiled binaries
*.exe
*.exe.old
*.exe~

# Local service configs (may contain tokens/credentials — live in %ProgramData%\Tela\ or /etc/tela/)
*-telad.yaml
*-telahubd.yaml

# Architecture Deep Dive

(Stub. Canonical at `DESIGN.md`. Book version: the architecture told as a
narrative, with the design decisions explained -- why three binaries, why
WireGuard in userspace, why a blind relay, why session-per-tunnel today
and channel multiplexing tomorrow, why YAML for config and JSON for the
API.)

# The Client

(Stub. CLI reference at `book/src/howto/services.md` and the corresponding
section of the main CLI reference. Book version: the local-bind model, port
remapping, profiles, environment variable defaults, the credential store.)

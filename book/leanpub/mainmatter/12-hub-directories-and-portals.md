# Hub directories and portals

(Stub. The mdBook version of this chapter at `book/src/guide/directories.md`
is the current canonical source. Book version: the directory protocol
(`/.well-known/tela`, `/api/hubs`), how `tela remote` and `telahubd portal`
use it, what a portal adds beyond the directory floor, and the two
operating models for running one. Awan Saya is named as one managed
implementation, available on request.)

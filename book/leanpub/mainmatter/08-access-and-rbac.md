# Access Control and RBAC

(Stub. Canonical design doc at `ACCESS-MODEL.md` (rendered as
`book/src/architecture/access-model.md` on the site). Book version should
walk through the access model from first principles, the four roles, the
per-machine permissions, what wildcards mean, the unified `/api/admin/access`
endpoint, and worked examples.)

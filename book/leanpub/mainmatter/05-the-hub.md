# The Hub

(Stub. The CLI/operator reference for telahubd is at
`book/src/howto/hub.md` and the corresponding section of the CLI reference.
For the book, expand into the design rationale: why the hub holds no
keys, how the auth model evolved, what "open mode" means and why it
defaults to closed, the portal sync model.)
